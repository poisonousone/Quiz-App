import 'package:flutter/material.dart';

abstract class Theme {
  Color _backgroundColor = Colors.grey.shade800;
  Color _appBarColor = Colors.grey.shade900;
  Color _textColor = Colors.white;
  Color _chosenColor = Colors.amber.shade600;
  Color _rightColor = Colors.green;
  Color _wrongColor = Colors.redAccent;
  Color _checkColor = Colors.green.shade900;
  Color _borderColor = Colors.grey.shade400;
  Color _panelColor = Colors.grey.shade700;
  _questionTextStyle = TextStyle(

  )

}